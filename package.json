{
    "scripts": {
        "start": "node server/server.js",
        "build": "cd client && npm install && npm run build"
    },
    "dependencies": {
        "axios": "^1.5.1",
        "cors": "^2.8.5",
        "express": "^4.18.2",
        "path": "^0.12.7"
    }
}
