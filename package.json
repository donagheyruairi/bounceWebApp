{
    "scripts": {
        "start": "node server/server.js",
        "build": "concurrently \" npm install \" \"cd client && npm install && npm run build\"",
        "test" : "concurrently \"cd client && npm run test\" \"cd server && npx jest server.test.js\""
    },
    "dependencies": {
        "axios": "^1.5.1",
        "cors": "^2.8.5",
        "express": "^4.18.2",
        "path": "^0.12.7"
    },
    "devDependencies": {
        "@babel/plugin-proposal-private-property-in-object": "^7.21.11",
        "jest": "^29.7.0",
        "supertest": "^6.3.3"
    }
}
